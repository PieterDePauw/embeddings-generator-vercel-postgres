// import { createOpenAI } from "@ai-sdk/openai"

// // Define a function to create a new OpenAI API client
// export const createOpenAIClient = (openaiKey: string) => createOpenAI({ apiKey: openaiKey, compatibility: "strict" })

// // Configure the OpenAI API client
// export const openai = createOpenAIClient(process.env.OPENAI_API_KEY)
